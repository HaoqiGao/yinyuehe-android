# 音悦盒验证记录 · 2026-07-14

## 结论与范围

本记录保留两个不同代码快照的证据，并明确限定各自范围：

```text
丰富人工基线：d9b04564b96987eb534c0c23f054a8662d89a4f1
最终 pre-evidence：deae6a828936b55a250bbad9b4dc09b4fabb1631
```

`d9b04564` 的人工设备采集窗口为 2026-07-14 08:34–08:39 UTC（北京时间 16:34–16:39），对应下文明确标为“基线”的权限、后台、通知、收藏、事件、重启、启动、内存和 5 轮稳定性记录。基线之后又完成缺失 MediaStore 卷的原子/单飞协调、API 26–28 `DISPLAY_NAME`、ended/idle/空队列播放按钮、起播耗时事件归因和进程级首帧等硬化。

`deae6a82` 的最终回归采集窗口约为 2026-07-14 09:53–09:59 UTC（北京时间 17:53–17:59），并于 10:01–10:02 UTC 补充 grant/revoke/regrant 权限回归；对应下文“完整门禁”“自动化结果”“最终硬化回归”和“APK 产物”。文档自身在该代码验证完成后提交，因此文档提交 SHA 会晚于最终 pre-evidence SHA。没有把 `d9b04564` 的旧人工测量改标成 `deae6a82` 的结果，也没有宣称最终快照重跑了全部人工基线。

结合两个快照且按上述边界解读，验收矩阵结果为 5 项 `AUTOMATED_PASS`、14 项 `MANUAL_PASS`、2 项 `PENDING_DEVICE`、0 项 `FAIL`。未执行的 AudioFocus 物理抢占和真实输出断开没有标记为通过。

## 证据保存方式

“完整门禁”和“自动化结果”保留了最终快照执行的完整 Gradle 命令；“媒体通知”也保留了基线查询命令。其余设备章节保存的是时间范围、操作顺序和关键输出摘录，不是每一步原始 shell 命令的逐字副本。完整终端 transcript 与原始 adb 日志没有提交到仓库，因此本文件是两次快照的审计记录，而不是可独立重放全部手工流程的脚本。

## 环境

| 项目 | 值 |
| --- | --- |
| 主机 | macOS 26.5.2, arm64 |
| JDK | Eclipse Temurin 17.0.19+10 |
| Gradle | 8.13（embedded Kotlin 2.0.21） |
| 工程工具链 | AGP 8.12.3；Kotlin plugin 2.0.20；Media3 1.10.1；Room 2.8.4 |
| Android SDK | platform 36；build-tools 36.0.0；command-line tools 21.0 |
| 设备 | `emulator-5554`；AVD `yinyuehe_api36`；API 36；`arm64-v8a`；Android SDK built for arm64 |

## 完整门禁

在仓库根目录执行：

```bash
export JAVA_HOME=/tmp/codex-jdk17/jdk-17.0.19+10/Contents/Home
export ANDROID_HOME=/tmp/android-sdk
export ANDROID_SDK_ROOT=/tmp/android-sdk
./gradlew test testDebugUnitTest lintDebug assembleDebug --stacktrace
```

最终 `deae6a82` 结果：exit code `0`，`BUILD SUCCESSFUL in 10s`，460 个 actionable tasks（148 executed、28 from cache、284 up-to-date）。当前 51 个 Gradle XML 报告文件汇总为 280 次 JVM 测试执行、0 failure、0 error、0 skipped；同一测试可能在 debug/release 变体报告中重复出现，因此 280 是执行次数，不是 280 个独立测试用例。

以下六个 lint 文本报告均为 `No issues found.`：`:app`、`:feature:library`、`:core:data`、`:core:player`、`:core:designsystem`、`:core:testing`。

CI 已核对：`.github/workflows/ci.yml` 在 pull request 与 `main` push 上安装 Temurin 17 和隔离的 SDK 36，并执行完全相同的 Gradle 命令；无需行为修改。

## 自动化结果

### API 36 设备测试

Room 迁移单独执行，避免 instrumentation class 参数过滤其他模块：

```bash
./gradlew :core:data:connectedDebugAndroidTest \
  -Pandroid.testInstrumentationRunnerArguments.class=app.yinyuehe.core.data.local.db.YinYueHeMigration1To2Test \
  --stacktrace
```

最终 `deae6a82` 结果：exit code `0`，`1/1`。

Compose/App 测试：

```bash
./gradlew :feature:library:connectedDebugAndroidTest \
  :app:connectedDebugAndroidTest --stacktrace
```

最终 `deae6a82` 结果：exit code `0`，`:feature:library` 为 `7/7`，`:app` 为 `4/4`，合计 `11/11`。

### 主要 JVM 覆盖

- MediaStore 稳定 ID、API 26–28 `DISPLAY_NAME`、元数据清洗、完整卷快照、缺失卷原子协调、单飞扫描、删除标记、查询失败不破坏旧缓存与取消传播。
- Room favorite Flow add/remove、recent 聚合/排序/20 条硬限制、Demo anchor 不混入本地曲库、未知 ID 拒绝。
- Media3 play/shuffle/transport/seek/queue 命令、命令可用性、回调快照、ended/idle/空队列播放语义、自然切歌完成语义与服务销毁前写队列排空。
- 三页 UDF、权限 grant/revoke/regrant、扫描取消、收藏快速双击串行化、拖动期间忽略位置 tick、队列重复 ID 稳定 key。
- 事件类型、500 条上限、起播耗时按实际事件来源与匹配曲目归因、进程级首帧、Recorder 失败不影响播放。

## 最终硬化回归

本节只记录最终 `deae6a82` debug APK 的补充回归，主要采集窗口约为 2026-07-14 09:53–09:59 UTC。它补充验证基线后修改的风险点，不表示下文所有 `d9b04564` 人工流程均已重跑。

### 权限切换

2026-07-14 10:01–10:02 UTC，在 API 36 模拟器对最终快照执行三段冷启动：

```text
grant READ_MEDIA_AUDIO + force-stop/cold launch:   本地曲库 / codex_resume_test
revoke + force-stop/cold launch:                   示例曲库 / 晨间节拍
regrant + force-stop/cold launch:                  本地曲库 / codex_resume_test
```

这是最终快照的关键状态摘录，不是完整 adb transcript；它验证硬化后授权曲库、撤权 Demo 投影和重新授权恢复仍可工作。Room 缓存细节仍引用下文 `d9b04564` 基线记录。

### 自然完播后的单击重播

启动四首 Demo 队列后，应用包对应的 MediaSession 首先为 `PLAYING`、item `0`。四首自然播放结束后，会话为 `STOPPED`、position `4009`、item `3`，UI 的内容描述/文本为可用的 `Play`。只点击一次后，会话成为 `PLAYING`、position `0`、item `3`。

这条执行记录验证 ended 状态不会把 UI 留在不可恢复的暂停语义，且当前曲目可单击从头重播；它不覆盖 repeat/shuffle 的所有组合，也不是长时播放证明。

### 同进程旋转的首帧去重

同一应用进程 PID `11459` 在旋转前后保持不变，Room 中 `FIRST_FRAME` 事件总数为 `2 → 2`。已有的 2 条事件来自此前两次不同的 force-stop 后进程启动；因此这里只把“同一 PID 配置重建没有新增事件”作为进程内去重证据，不把总数 2 解释为全局 exactly-once。

以上两条人工回归及权限切换都只保存了关键状态和操作摘要，没有提交完整终端 transcript 或原始 adb 日志。

## 权限与 MediaStore

> 本节为 `d9b04564` 丰富人工基线，不是 `deae6a82` 全量复跑。最终快照的权限切换补充见[最终硬化回归](#最终硬化回归)。

时间：2026-07-14 08:37:30–08:38:01 UTC。先安装基线 APK 并清空应用数据。模拟器 `/sdcard/Music/codex_resume_test.wav` 已被 MediaStore 识别为 `_id=24`、`is_music=1`、时长 3200ms。

执行流程：

1. 无 `READ_MEDIA_AUDIO` 冷启动：`Status: ok`、`LaunchState: COLD`，UI 显示“示例曲库”和“晨间节拍”。
2. `pm grant app.yinyuehe android.permission.READ_MEDIA_AUDIO`，HOME 后重新进入 Activity：UI 自动切换为“本地曲库”，显示 `codex_resume_test`。
3. 设备 Room 查询得到：

```text
mediaId                             title              volumeName        isAvailable
local:v1:ZXh0ZXJuYWxfcHJpbWFyeQ:24  codex_resume_test  external_primary  1
```

4. `pm revoke`、`am force-stop`、冷启动：`Status: ok`、`LaunchState: COLD`、`TotalTime: 706`，UI 只显示 Demo；Room 查询仍保留 1 条 available 本地缓存，但无权限 UI 不暴露它。
5. 再次 grant 并触发 `onResume`：UI 恢复“本地曲库/codex_resume_test”。

这同时验证了权限状态与曲库集合的原子投影，以及撤权不删除用户缓存。

## 后台播放与系统控制

> 本节为 `d9b04564` 丰富人工基线，不是 `deae6a82` 全量复跑。

2026-07-14 08:34:36 UTC，在 MainActivity 已处于 `mLastPausedActivity`（后台）的情况下，通过系统媒体按键依次执行 play、pause、play；`dumpsys media_session` 摘录：

```text
after PLAY:        state=PLAYING position=0   speed=1.0 active item id=3
after PAUSE:       state=PAUSED  position=402 speed=0.0 active item id=3
after resume PLAY: state=PLAYING position=406 speed=1.0 active item id=3
```

2026-07-14 08:38:47 UTC 又执行 5 轮冷启动 → 点击 Demo 播放全部 → HOME → 系统 pause → 系统 play；每轮均为：

```text
cycle=1..5 launch=ok initial=PLAYING paused=PAUSED resumed=PLAYING pid=<live>
```

短 Demo 队列还实际产生自动切歌后的 changed/completed/history 记录。上述证据验证后台服务、系统控制和状态同步；不把系统媒体按键当作 AudioFocus 或真实耳机断开证据。

## 媒体通知

> 本节为 `d9b04564` 丰富人工基线，不是 `deae6a82` 全量复跑。

2026-07-14 08:39:07 UTC，在会话处于 `PLAYING` 时执行：

```bash
adb -s emulator-5554 shell dumpsys notification --noredact
```

关键输出：

```text
pkg=app.yinyuehe id=1001 importance=2 category=transport
flags=ONLY_ALERT_ONCE|NO_CLEAR|FOREGROUND_SERVICE
android.title=轻回声
android.text=音悦盒 Demo Band
android.template=android.app.Notification$MediaStyle
androidx.media3.session=Bundle (...)
android.mediaSession=Token (...)
```

同一时刻 MediaSession `active=true` 且 `state=PLAYING`。

## 收藏与重启

> 本节为 `d9b04564` 丰富人工基线，不是 `deae6a82` 全量复跑。

2026-07-14 08:35:26 UTC，点击“收藏晨间节拍”后直接查询设备 Room：

```text
favorites:       demo:morning-pulse, addedAtEpochMs=1784018125982
playback_events: FAVORITE_CHANGED, demo:morning-pulse
```

随后 `am force-stop app.yinyuehe` 并 `am start -W`：`Status: ok`、`LaunchState: COLD`、`TotalTime: 782`。重启后同一 favorite 行仍存在，recent 行为 4 条，`user_version=2`；UI 语义为“取消收藏晨间节拍”。

## 事件证据

> 本节中的设备数据库值来自 `d9b04564` 丰富人工基线；`deae6a82` 的起播归因与进程级首帧硬化另由最终 JVM/设备回归覆盖。

通过设备内 `/system/bin/sqlite3` 查询 `playback_events`，实际记录包含：

```text
PLAY_REQUESTED      demo:morning-pulse
TRACK_CHANGED       demo:morning-pulse / city-walk / soft-echo / night-drive
PLAYBACK_STARTED    demo:morning-pulse / city-walk / soft-echo / night-drive
PLAYBACK_COMPLETED  demo:morning-pulse / city-walk / soft-echo / night-drive
FAVORITE_CHANGED    demo:morning-pulse
FIRST_FRAME         durationMs=365
PLAY_START_LATENCY  demo:morning-pulse durationMs=312
```

事件仅写入本地 Room，当前实现没有上传链路。每行持久化 typed name、可选稳定 track ID、发生时间和可选 duration；没有标题、文件名、路径或 URI。稳定 track ID 在本地数据库中仍可关联同一曲目，因此这里不使用“匿名数据”表述。JVM 测试验证事件 newest-first、501 条写入只裁掉最旧行、自然/重复切歌的完成顺序，以及 first matching callback 才消费起播计时。

## 迁移与重启

最终 `deae6a82` 的 `YinYueHeMigration1To2Test` 在 API 36 模拟器真实执行 `1/1`：从导出的 v1 schema 创建数据库，写入 v1 用户数据，执行 1→2 migration，确认新增 `playback_events` 且旧数据不变。

`d9b04564` 基线的应用侧另通过 force-stop 冷启动确认 favorite、recent 和 `user_version=2` 仍存在；这部分没有改标为最终快照复跑。迁移自动测试和基线进程重启是两条独立证据。

## 启动基线

> 本节为 `d9b04564` 丰富人工基线，不是 `deae6a82` 的性能复测。

2026-07-14 08:35:43 UTC，在 API 36 arm64 模拟器连续执行 5 次 `am force-stop` + `am start -W`：

| 轮次 | Status | LaunchState | TotalTime |
| --- | --- | --- | --- |
| 1 | ok | COLD | 774ms |
| 2 | ok | COLD | 792ms |
| 3 | ok | COLD | 800ms |
| 4 | ok | COLD | 776ms |
| 5 | ok | COLD | 798ms |

均值 788ms，范围 774–800ms。该样本没有使用 Macrobenchmark、release/profileable 构建、预热控制或多设备对照，因此只作为 `d9b04564` 在当前模拟器上的 debug 冷启动基线，不能宣称已完成启动优化，也不能外推为 `deae6a82` 的时延。

## 内存快照

> 本节为 `d9b04564` 丰富人工基线，不是 `deae6a82` 的内存复测。

2026-07-14 08:39:02 UTC，经过 5 轮播放/后台/系统控制后，MediaSession 仍为 `PLAYING`（position 106ms，active item id 1），执行 `dumpsys meminfo app.yinyuehe`：

```text
Java Heap PSS: 17,864KB
Native Heap PSS: 13,940KB
Private Other: 40,340KB
System: 14,871KB
TOTAL PSS: 89,495KB
TOTAL RSS: 224,632KB
TOTAL SWAP: 0KB
```

这是 `d9b04564` 已采集的内存单点基线，只证明采样时进程存活；没有 heap dump、GC 趋势或长时序列，不能据此声称无泄漏或已完成内存优化，也不能外推为 `deae6a82` 的内存值。

## 稳定性循环

> 本节为 `d9b04564` 丰富人工基线；最终 `deae6a82` 另有[最终硬化回归](#最终硬化回归)，但没有重跑这套 5 轮流程。

2026-07-14 08:38:47 UTC，清空 logcat 后执行 5 轮经状态断言的 Demo 流程：冷启动、起播、退后台、系统暂停、系统恢复。5/5 的 launch 为 `ok`，状态均为 `PLAYING → PAUSED → PLAYING`，每轮结束 PID 仍存活；过滤：

```text
FATAL EXCEPTION
ANR in app.yinyuehe
am_anr ... app.yinyuehe
```

结果为 `none`。这是一轮短时 smoke，不替代长时间 soak、低内存回收或厂商 ROM 回归。

## APK 产物

`app/build/outputs/apk/debug/app-debug.apk`：

```text
size:   14,201,312 bytes
sha256: 183d5f13bae95485884e92882a8305c657e459437e7cb6d5368ca66636eef949
```

这是最终 `deae6a82` 在本记录环境与本机 debug signing key 下生成并捕获的 debug APK；clean source build 与两次 rerun 在本机得到相同字节。该 SHA-256 只标识这份本地产物，不承诺跨机器或 keystore 字节复现；它不是带正式签名的 Release APK。

## 物理设备待验

### AudioFocus 抢占

当前 `PlaybackService` 通过 `setAudioAttributes(..., handleAudioFocus=true)` 交给 Media3 管理；尚需在物理设备执行：

1. 音悦盒播放中，用另一个真实媒体/通话应用触发 transient、duck 和 permanent focus change。
2. 记录音量、暂停/恢复策略、MediaSession 状态和通知状态。
3. 覆盖 Android 版本与至少一个厂商 ROM 后再改为 PASS。

### Becoming noisy

当前已配置 `setHandleAudioBecomingNoisy(true)`；尚需在物理设备分别验证：

1. 有线耳机播放时拔出。
2. 蓝牙耳机播放时主动断开或走出范围。
3. 确认扬声器不意外继续播放，并记录通知/MediaSession 的 PAUSED 状态。

## 证据边界

- 未验证在线歌词或在线元数据；当前也没有实现这些功能。
- 未执行正式 Release 签名、Play 发布或物理设备完整回归。
- 未执行 Macrobenchmark/Baseline Profile 对照、Memory Profiler 泄漏分析或长时 soak；启动、内存与稳定性数据均按本记录的有限范围解读。
